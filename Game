require 'spec_helper'

class Team

 def add_player(name, position)
      def add_player(name, position)
      i = position
      for i in var
      @player[i] = name
      end
 end
 end
 
 def remove_player(name)
   for i in var
      if @player[i] == name
       @player.delete
      end
      end
 end

 def filter_by_position(position)
    #return players in the given position
    puts (@player[position])
 end

 def to_s(positio)
  # output team name + players list
   for i in
   puts @player[i].to_s + "-" + position
   end
end

describe Team do

end
